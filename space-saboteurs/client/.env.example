# URL of the deployed Socket.IO game server (the persistent backend on
# Render/Railway/Fly.io — NOT a Vercel serverless function).
# Set this as a Vercel Environment Variable for the client project;
# scripts/generate-env.js bakes it into env.js at build time.
NEXT_PUBLIC_GAME_SERVER_URL=http://localhost:3001
